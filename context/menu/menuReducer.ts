/*import React from "react";
import { MenuState } from "./MenuProvider";

type MenuActionType = { type: "Change-Menu"; payload: string[] };

export const menuReducer = (state: MenuState, action: MenuActionType) => {
  switch (action.type) {
    case "Change-Menu":
      return {
        ...state,
        //men: [...state.entries, action.payload]
      };

    default:
      break;
  }
};
*/
